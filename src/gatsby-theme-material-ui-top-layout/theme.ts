import { createMuiTheme } from "@material-ui/core"

const theme = createMuiTheme({
  //   palette: {
  //     type: 'dark',
  //   },
  palette: { primary: { main: "#171b29" }, secondary: { main: "#dde0e6" } },
  typography: {
    fontSize: 18,
  },
})

export default theme
