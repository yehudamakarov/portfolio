import { createMuiTheme } from "@material-ui/core"

const theme = createMuiTheme({
  //   palette: {
  //     type: 'dark',
  //   },
  palette: { primary: { main: "#171b29" }, secondary: { main: "#a1a4aa" } },
  typography: {
    fontSize: 18
  }
})

export default theme